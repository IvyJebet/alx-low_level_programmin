#include "main.h"
/**
 * print_numbers - checks for checks for a digit (0 to 9)
 * 
 * Return: Always 0
 */
void print_numbers(void)
{
	int i;

	for (i = 48; i < 58; i++)
	{
		_putchar(c);
	}
	_putchar('\n');
}
