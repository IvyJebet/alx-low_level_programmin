#include "main.h"
/**
 * mul - Multiplies two integers
 * ends in a new line
 *@a: a - Variable
 @b: b - Variable
 * Return: Always 0
 */
int mul(int a, int b)
{
	int mul;

	mul = a*b;

	return (mul);
}
