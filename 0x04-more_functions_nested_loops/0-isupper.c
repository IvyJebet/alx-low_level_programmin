#include "main.h"
/**
 * _isupper - Checks for uppercase character
 * @c: Variable text
 * ends in a new line
 *
 * Return: Always 0
 */
int _isupper(int c)
{

	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
		else
		{
			return (0);
		}
}
